apiVersion: v1
kind: Secret
metadata:
  name: kubemate-secrets
  labels:
    app: kubemate
type: Opaque
stringData:
  # IMPORTANT: Change these values before deploying to production!
  # Generate secure random values for production use
  db-user: "kubemate"
  db-password: "changeme-in-production"
  flask-secret-key: "changeme-in-production-use-openssl-rand-hex-32"
  encryption-key: "changeme-in-production-use-openssl-rand-hex-32"
